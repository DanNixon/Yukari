language: cpp

dist: trusty
sudo: required

before_install:
  - sudo add-apt-repository ppa:v-launchpad-jochen-sprickerhof-de/pcl -y
  - sudo apt-get -qq update
  - sudo apt-get install -y cmake libboost-all-dev libpcl-all
  - cmake --version

script:
  - mkdir -p build
  - cd build
  - cmake .. -DCMAKE_BUILD_TYPE=Debug -DBOOST_LIBRARYDIR=/usr/lib/x86_64-linux-gnu/
  - make -j4
  - ctest -VV

notifications:
  email:
    on_success: change
    on_failure: change
